"use client";
import CreateCourse from "../Createcourse";

export default function CourseTabPage() {
  // Removed unused variables tab and courseId

  // This handles routes like /dashboard/courses/courseStructure (no courseSlug)
  return <CreateCourse />;
}