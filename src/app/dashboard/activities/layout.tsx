import { metadata } from "./metadata";
export { metadata };

export default function ActivitiesLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Example: Render children or specific components as needed */}
      {children}
    </div>
  );
}